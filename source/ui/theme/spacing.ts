// Spacing scale and standard widths.
//
// Minimal-mono leans on whitespace for grouping, so a small, consistent spacing
// scale keeps breathing room uniform across screens.

export const space = {
	none: 0,
	xs: 1,
	sm: 2,
	md: 3,
} as const;

export const width = {
	sidebar: 34,
	mediaPane: '50%',
} as const;
